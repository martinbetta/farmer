<div class="rate-stars-box">
  <% 5.times do |index| %>
    <i class="<%= avg_score > index ? 'fas' : 'far' %> fa-star <%= avg_score > index ? 'text-success' : 'text-muted' %>" ></i>
  <% end %>
</div>
